# 9. Given an undirected graph with costs, find a Hamiltonian cycle of low cost (approximate TSP) by using the
# heuristic of sorting the edges in increasing order of their costs and, for each edge, choose it if and only if it
# does not close a cycle of length lower than n.
from domain import Graph
from service2 import Service
from ui import UI


def service2(g):
    pass


if __name__ == "__main__":
    ui = UI()
    ui.run()
