# P4 - 5. Write a program that, given an undirected connected graph,
# constructs a minumal spanning tree using the Kruskal's algorithm.

from ui import UI

if __name__ == "__main__":
    ui = UI()
    ui.run()
